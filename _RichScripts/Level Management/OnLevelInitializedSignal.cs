using RichPackage.Events.Signals;

namespace RichPackage.Events
{
    /// <summary>
    /// Raised when the level has initialized but before level load.
    /// </summary>
    public sealed class OnLevelInitializedSignal : ASignal
    {
        // exists
    }
}
